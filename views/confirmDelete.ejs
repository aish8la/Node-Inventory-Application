<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <form action="<%= path %>" method="POST">
        <h1><%= message %></h1>
        <% if (isProtected) { %>
            <p>Password is required to perform the operation on Protected Items</p>
            <label for="password">Password</label>
            <input type="password" name="password" id="password">
        <% } %>
        <button id="back-button" type="button" onclick="history.back(); return false;">Back</button>
        <button type="submit">Delete</button>
    </form>
</body>
</html>